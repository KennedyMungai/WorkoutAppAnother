import { View, Text } from 'react-native'

type Props = {}

const WorkoutDetailScreen = (props: Props) => {
	return (
		<View>
			<Text>Workout Detail Screen</Text>
		</View>
	)
}

export default WorkoutDetailScreen
